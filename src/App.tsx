import {SafeAreaView, View} from 'react-native';
import React from 'react';

const App: React.FC = () => {
    return (
        <SafeAreaView>
            <View></View>
        </SafeAreaView>
    );
};

export default App;
